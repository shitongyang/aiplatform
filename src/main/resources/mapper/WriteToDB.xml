<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.iscas.aiplatform.mapper.WriteToDBMapper">

    <insert id="writeToOutputFormat">
        insert into output_format(username,output_format_path,output_format_des,create_time) values(#{username},#{path},#{describe},NOW())
    </insert>

</mapper>